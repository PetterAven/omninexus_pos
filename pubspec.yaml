name: omninexus_pos
description: "Sistema Punto de Venta OmniNexus para Windows"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Motor de base de datos nativo para Escritorio (FFI)
  sqflite_common_ffi: ^2.4.2
  excel: ^4.0.6
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true